# RAG API — OpenAPI-совместимое описание (упрощённое)

Сервер: `python tools/rag_api.py --port 8765` (или `make serve`).
Интерфейс совместим с простыми HTTP-клиентами и агентами; полные детали —
в `tools/rag_api.py` (workspace-скрипт).

```json
{
  "openapi": "3.0.3",
  "info": {
    "title": "RAG API курса «История и философия науки и технологий»",
    "version": "1.0.0",
    "description": "Семантический поиск по верифицированному корпусу источников. Ответы содержат цитируемые фрагменты с координатами «файл · фрагмент #N»."
  },
  "servers": [ { "url": "http://127.0.0.1:8765" } ],
  "paths": {
    "/health": {
      "get": {
        "summary": "Проверка работоспособности",
        "responses": { "200": { "description": "OK" } }
      }
    },
    "/search": {
      "get": {
        "summary": "Семантический поиск по корпусу",
        "parameters": [
          { "name": "q", "in": "query", "required": true, "schema": { "type": "string" },
            "description": "Запрос (RU/EN)" },
          { "name": "k", "in": "query", "schema": { "type": "integer", "default": 5 },
            "description": "Число результатов" },
          { "name": "topic", "in": "query", "schema": { "type": "string" },
            "description": "Фильтр по теме (история_математики и т.п.)" },
          { "name": "threshold", "in": "query", "schema": { "type": "number", "default": 0.0 },
            "description": "Минимум score" }
        ],
        "responses": {
          "200": {
            "description": "Результаты: чанки с текстом, file, chunk_id, score",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "query": { "type": "string" },
                    "count": { "type": "integer" },
                    "results": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "score": { "type": "number" },
                          "file": { "type": "string" },
                          "topic": { "type": "string" },
                          "chunk_id": { "type": "integer" },
                          "snippet": { "type": "string" }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/docs": {
      "get": { "summary": "Описание API", "responses": { "200": { "description": "OK" } } }
    }
  }
}
```

Пример вызова:

```bash
curl "http://127.0.0.1:8765/search?q=%D0%9D%D1%8C%D1%8E%D1%82%D0%BE%D0%BD%20%D0%97%D0%B0%D0%BA%D0%BE%D0%BD%D1%8B&k=3"
```