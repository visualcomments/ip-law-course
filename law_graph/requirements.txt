# Зависимости пакета «закон-граф» (law_graph)
# Базовый конвейер работает только на pydantic; numpy — для ускоренной
# обучаемой модели и метрик (если numpy недоступен, модель работает в
# скалярном fallback-режиме).

pydantic>=2.0
numpy>=1.24

# Опционально (для backend 'pyg' в link_prediction.py): реальная TGN-память
# torch>=2.0
# torch-geometric